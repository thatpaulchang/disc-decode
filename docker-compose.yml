services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - db-data:/data
    environment:
      DB_PATH: /data/disc-decode.db
      SESSION_SECRET: ${SESSION_SECRET:-dev-only-insecure-secret-change-for-prod}

volumes:
  db-data:
