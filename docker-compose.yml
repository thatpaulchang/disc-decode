services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - db-data:/data
    environment:
      DB_PATH: /data/disc-decode.db
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET must be set}

volumes:
  db-data:
